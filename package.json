{
  "name": "conduit",
  "private": true,
  "scripts": {
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "dev": "pnpm dev:client",
    "build": "pnpm --filter client build",
    "clean": "pnpm -r clean"
  }
}
